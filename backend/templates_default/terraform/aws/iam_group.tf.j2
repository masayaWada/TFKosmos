resource "aws_iam_group" "{{ resource_name }}" {
  name = "{{ group.group_name }}"
  path = "{{ group.path }}"
{% if group.tags %}
  tags = {
{% for key, value in group.tags.items() %}
    "{{ key }}" = "{{ value }}"
{% endfor %}
  }
{% endif %}
}

