{
  "name": "tfkosmos-app",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "cd src-tauri && CI=false TAURI_CI=false npx tauri dev",
    "tauri:dev": "cd src-tauri && CI=false TAURI_CI=false npx tauri dev",
    "build": "cd src-tauri && CI=false TAURI_CI=false npx tauri build",
    "tauri:build": "cd src-tauri && CI=false TAURI_CI=false npx tauri build",
    "tauri": "cd src-tauri && npx tauri",
    "clean": "cd src-tauri && cargo clean"
  },
  "description": "TFKosmos Desktop Application (Tauri)",
  "private": true
}